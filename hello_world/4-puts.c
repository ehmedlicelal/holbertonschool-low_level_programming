#include <stdio.h>

/**
 * main - this is for printing "Programming is like building a multilingual puzzle"
 * return (0) - if program runs with no errors it gives code 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
