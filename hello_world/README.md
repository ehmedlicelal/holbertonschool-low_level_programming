
Hello World

Introductory exercises that demonstrate compiling and running simple C programs. Tasks cover using the preprocessor, assembler, compiler, and basic I/O functions.

Files and descriptions
- `0-preprocessor`: Exercise about using the C preprocessor (task spec placeholder).
- `1-compiler`: Exercise about compiling C code (task spec placeholder).
- `2-assembler`: Exercise about generating assembly output.
- `3-name`: Simple executable that prints the program name.
- `4-puts.c`: Prints a string using `puts`.
- `5-printf.c`: Demonstrates `printf` usage.
- `6-size.c`: Prints sizes of C types on the current platform.
- `100-intel`: Example file to show Intel syntax (task placeholder).
- `101-quote.c`: Prints a famous programming quote and exits.

Usage
- These are small, single-purpose exercises—compile with `gcc` and run the produced executables to observe output.
